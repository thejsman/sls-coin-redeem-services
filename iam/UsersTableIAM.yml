UsersTableIAM:
  Effect: Allow
  Action:
    - dynamodb:PutItem
    - dynamodb:GetItem
    - dynamodb:Scan
  Resource:
    - arn:aws:dynamodb:#{AWS::Region}:#{AWS::AccountId}:table/UsersTable
    - arn:aws:dynamodb:#{AWS::Region}:#{AWS::AccountId}:table/BufferCoinsTable
